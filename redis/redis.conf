# Redis runs here as an internal broker for background jobs.
# Use noeviction so RQ enqueue/dequeue operations fail explicitly
# instead of silently dropping job data under memory pressure.
maxmemory-policy noeviction
