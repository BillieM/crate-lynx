import "./App.css";

function App() {
  return (
    <main className="app">
      <section className="hero">
        <p className="eyebrow">crate-lynx</p>
        <h1>Frontend scaffold ready for the playlist UI.</h1>
        <p className="body">
          This Vite + React + TypeScript app is the starting point for the
          upcoming shell, routing, and data-fetching work in `app-ui/`.
        </p>
      </section>
    </main>
  );
}

export default App;
